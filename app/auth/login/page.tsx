import LoginForm from "../_component/LoginForm";

const page = () => {
  return (
    <section className="flex items-center justify-center">
      <LoginForm />
    </section>
  );
};

export default page;
